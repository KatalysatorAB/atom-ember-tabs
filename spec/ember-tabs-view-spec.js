'use babel';

import EmberTabsView from '../lib/ember-tabs-view';
console.log(EmberTabsView);
describe('EmberTabsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
