module.exports = {
  overrideUsePods: {
    description: 'Override usePods config from `.ember-cli` file and always force pod tab behavior.',
    type: 'boolean',
    default: false,
    order: 1
  }
};
